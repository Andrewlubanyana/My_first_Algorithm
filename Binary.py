def binary_algo(list, target):
  first = 0
  last = len(list) - 1
  
  while first <= last
    midpoint = len(first + last) // 2
  if [midpoint] == target:
    return midpoint
  elif list [midpoint] < target:
    first = midpoint + 1
  else:
    last = midpoint - 1
    
    
